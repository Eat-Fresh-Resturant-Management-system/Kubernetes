apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sql-pvc
  namespace: eatfresh
spec:
  storageClassName: "local-path"
  volumeName: sql-pv
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi  